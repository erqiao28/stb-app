获取行记录透视数据
获取工作表记录透视表汇总数据，适用于数据分析统计场景，与统计中透视表功能一致

path Parameters
worksheet_id
required
string
工作表ID

header Parameters
HAP-Appkey
required
string
HAP应用密钥

HAP-Sign
required
string
HAP签名

Request Body schema: application/json
required
pageSize	
integer <= 1000
Default: 100
每页数量，最大为1000

pageIndex	
integer >= 1
Default: 1
页码

viewId	
string
视图ID

columns	
Array of objects (pivotDimension)
列（维度）字段列表

Array 
field
required
string
字段ID

displayName	
string
显示名称

granularity	
integer
日期、地区统计维度 字段类型是日期或者地区时，通过此参数设置统计归组粒度

日期时间：1-日，2-周，3-月
地区：1-省，2-省/市，3-省/市/县（区）
includeEmpty	
boolean
Default: false
是否统计空值，默认为false不统计

rows	
Array of objects (pivotDimension)
行（维度）字段列表

Array 
field
required
string
字段ID

displayName	
string
显示名称

granularity	
integer
日期、地区统计维度 字段类型是日期或者地区时，通过此参数设置统计归组粒度

日期时间：1-日，2-周，3-月
地区：1-省，2-省/市，3-省/市/县（区）
includeEmpty	
boolean
Default: false
是否统计空值，默认为false不统计

values
required
Array of objects (pivotValue)
值字段列表

Array 
field
required
string
字段ID，仅统计记录行数时，使用record_count作为字段ID

displayName	
string
显示名称

aggregation
required
string
Enum: "COUNT" "DISTINCTCOUNT" "SUM" "MIN" "MAX" "AVG"
聚合方式（不区分大小写）

COUNT：计数
DISTINCTCOUNT：去重计数
SUM：求和
MIN：最小值
MAX：最大值
AVG：平均值
includeEmpty	
boolean
Default: false
是否统计空值，默认为false不统计，空值将显示为0

filter	
object (filter-2)
筛选器配置

type
required
string
Enum: "group" "condition"
筛选类型，当type为condition时，field/operator/value为必填；当type为group时，logic为必填

logic	
string
Enum: "AND" "OR"
逻辑操作符，不区分大小写

field	
string
字段ID或别名（当type为condition时必填）

operator	
string
运算符（当type为condition时必填）

value	
any
条件值（当type为condition时必填）

children	
Array of objects
子筛选条件（当type为group时使用）

Array 
type
required
string
Enum: "group" "condition"
筛选类型，当type为condition时，field/operator/value为必填；当type为group时，logic为必填

logic	
string
Enum: "AND" "OR"
逻辑操作符，不区分大小写

field	
string
字段ID或别名（当type为condition时必填）

operator	
string
运算符（当type为condition时必填）

value	
any
条件值（当type为condition时必填）

children	
object (filter-2) Recursive
筛选器配置

sorts	
Array of objects
排序字段

Array 
field
required
string
字段ID

isAsc
required
boolean
是否升序，默认为降序

includeSummary	
boolean
Default: false
返回值是否包含所有行汇总值

Responses
200
成功获取透视表数据

Response Schema: application/json
success	
boolean
是否调用成功

error_code	
integer
错误代码

error_msg	
string
错误消息

data	
object
meta
required
object (pivotMeta)
透视表元数据

columns	
Array of objects (pivotDimension)
列（维度）字段列表

Array 
field
required
string
字段ID

displayName	
string
显示名称

granularity	
integer
日期、地区统计维度 字段类型是日期或者地区时，通过此参数设置统计归组粒度

日期时间：1-日，2-周，3-月
地区：1-省，2-省/市，3-省/市/县（区）
includeEmpty	
boolean
Default: false
是否统计空值，默认为false不统计

rows	
Array of objects (pivotDimension)
行（维度）字段列表

Array 
field
required
string
字段ID

displayName	
string
显示名称

granularity	
integer
日期、地区统计维度 字段类型是日期或者地区时，通过此参数设置统计归组粒度

日期时间：1-日，2-周，3-月
地区：1-省，2-省/市，3-省/市/县（区）
includeEmpty	
boolean
Default: false
是否统计空值，默认为false不统计

values
required
Array of objects (pivotValue)
值字段列表

Array 
field
required
string
字段ID，仅统计记录行数时，使用record_count作为字段ID

displayName	
string
显示名称

aggregation
required
string
Enum: "COUNT" "DISTINCTCOUNT" "SUM" "MIN" "MAX" "AVG"
聚合方式（不区分大小写）

COUNT：计数
DISTINCTCOUNT：去重计数
SUM：求和
MIN：最小值
MAX：最大值
AVG：平均值
includeEmpty	
boolean
Default: false
是否统计空值，默认为false不统计，空值将显示为0

pivot
required
Array of objects (pivotData)
透视表数据

Array 
columns	
object
列数据，字段ID对应的值

property name*
additional property
string
rows	
object
行数据，字段ID对应的值

property name*
additional property
string
values	
object
值数据，字段ID对应的计算值

property name*
additional property
number
summary	
object
汇总值，字段ID对应的汇总值

property name*
additional property
number
totalPages
required
integer
总页数

currentPage
required
integer
当前页码


post
/v3/app/worksheets/{worksheet_id}/rows/pivot
Request samples
Payload
Content type
application/json

Copy
Expand allCollapse all
{
"pageSize": 100,
"pageIndex": 1,
"viewId": "string",
"columns": [
{
"field": "string",
"displayName": "string",
"granularity": 0,
"includeEmpty": false
}
],
"rows": [
{
"field": "string",
"displayName": "string",
"granularity": 0,
"includeEmpty": false
}
],
"values": [
{
"field": "string",
"displayName": "string",
"aggregation": "COUNT",
"includeEmpty": false
}
],
"filter": {
"type": "group",
"logic": "AND",
"field": "string",
"operator": "string",
"value": null,
"children": [
{
"type": "group",
"logic": "AND",
"field": "string",
"operator": "string",
"value": null,
"children": { }
}
]
},
"sorts": [
{
"field": "string",
"isAsc": true
}
],
"includeSummary": false
}
Response samples
200
Content type
application/json

Copy
Expand allCollapse all
{
"success": true,
"error_code": 0,
"error_msg": "string",
"data": {
"meta": {
"columns": [
{
"field": "string",
"displayName": "string",
"granularity": 0,
"includeEmpty": false
}
],
"rows": [
{
"field": "string",
"displayName": "string",
"granularity": 0,
"includeEmpty": false
}
],
"values": [
{
"field": "string",
"displayName": "string",
"aggregation": "COUNT",
"includeEmpty": false
}
]
},
"pivot": [
{
"columns": {
"property1": "string",
"property2": "string"
},
"rows": {
"property1": "string",
"property2": "string"
},
"values": {
"property1": 0,
"property2": 0
}
}
],
"summary": {
"property1": 0,
"property2": 0
},
"totalPages": 0,
"currentPage": 0
}
}
